# CITYMO CIN OCR — dépendances de base (toujours)
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
numpy==1.26.4
opencv-python-headless==4.10.0.84
Pillow==11.1.0
pytesseract==0.3.13
httpx==0.28.1
pytest==8.3.4

# Optionnel (prod) — décommenter ou installer séparément :
# paddlepaddle==2.6.2
# paddleocr==2.9.1
